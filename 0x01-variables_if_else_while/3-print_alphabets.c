#include <stdio.h>

/**
 * main - prints the alphabet in lowercase, and then in uppercase.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;

	char b;
	while
		(a <= 'z') {
			putchar(a);
			a++;
		}
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
